import Link from "next/link"; import { notFound } from "next/navigation"; import { prisma } from "@/lib/prisma"; import { parseFormatted } from "@/lib/document"; import { DocumentView } from "@/components/document-view"; import { SendDocument } from "@/components/send-document";
export const dynamic = "force-dynamic";
export default async function CreatorDocument({params}:{params:Promise<{id:string}>}){const {id}=await params;const document=await prisma.document.findUnique({where:{id}});if(!document)notFound();return <main className="doc-shell"><div className="toolbar"><Link className="logo" href="/"><i/>DocFlow</Link><span className={`status ${document.status.toLowerCase()}`}>{document.status}</span></div><DocumentView document={parseFormatted(document.formattedJson)} signerName={document.signerName} signedAt={document.signedAt}/><div style={{marginTop:18}}><SendDocument id={document.id} token={document.shareToken} status={document.status}/></div></main>}
