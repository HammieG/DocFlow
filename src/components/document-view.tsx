import type { FormattedDocument } from "@/lib/document";
export function DocumentView({ document, signerName, signedAt }: { document: FormattedDocument; signerName?: string | null; signedAt?: Date | null }) { return <article className="document"><div className="type">DOCFLOW DOCUMENT</div><h1>{document.title}</h1>{document.preamble&&<p className="preamble">{document.preamble}</p>}{document.sections.map((section,index)=><section key={`${section.heading}-${index}`}><h2>{section.heading}</h2><p>{section.body}</p></section>)}{signerName&&<div className="signature"><div className="signature-name">{signerName}</div><p>Electronically signed by {signerName}{signedAt ? ` on ${signedAt.toLocaleString()}` : ""}.</p></div>}</article> }
